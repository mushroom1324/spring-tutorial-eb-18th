version: "3.8"
services:
  backend:
    image: "popcorn6282/spring-boot-tutorial-18th"
    ports:
      - "8080:8080"
    restart: "always"

  nginx:
    image: "popcorn6282/nginx"
    ports:
      - "80:80"
    restart: "always"
    depends_on:
      - "backend"